pub fn execute(input: &str) -> u32 {
    todo!()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = execute(
            "
            ",
        );

        assert_eq!(result, 0);
    }
}
